[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "holstein-parc"
version = "0.1.0"

[tool.setuptools]
packages = ["src"]
package-dir = { "src" = "src" }